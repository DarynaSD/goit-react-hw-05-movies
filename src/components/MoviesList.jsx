import React from 'react'

const MoviesList = () => {
  return (
    // <ul>
    //   {trending.map(one => (
    //     <li>
    //       <img src={one.url} alt={one.alt} />
    //     </li>
    //   ))}
      // </ul>
      <div>Movie List</div>
  );
}

export default MoviesList

